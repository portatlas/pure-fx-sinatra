$(document).ready(function () {
  // $("body").on("click", ".fx-rate", function(event){
  //   event.preventDefault();
    // console.log("no - click")
    // var amount = $(this).parent().siblings()[2].innerHTML;
    // var curr_have = $(this).parent().siblings()[3].innerHTML;
    // var curr_want = $(this).parent().siblings()[4].innerHTML;
    // console.log(curr_have)
    // console.log(curr_want)

    //     endpoint = 'live';


    //     $.ajax({
    //     url: 'http://apilayer.net/api/' + endpoint + '?access_key=' + access_key,
    //     dataType: 'jsonp',
    //     success: function(json) {

    //         // exchange rata data is stored in json.quotes
    //         alert(json.quotes.USDGBP);

    //         // source currency is stored in json.source
    //         alert(json.source);

    //         // timestamp can be accessed in json.timestamp
    //         alert(json.timestamp);

    //     }
    // });
})
});
